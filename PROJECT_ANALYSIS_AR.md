# تقرير تحليل مشروع `nwebot-railway`

## الملخص التنفيذي

المشروع عبارة عن بوت Telegram تعليمي يعتمد على Gemini لتوليد ملازم HTML/PDF وكويزات تفاعلية، ويدعم النصوص والصور والصوت وروابط YouTube وملفات PDF/HTML. البنية الحالية تحتوي على وظائف كثيرة ومفيدة، لكنها تجمع معظم منطق التطبيق داخل `index.js` بحجم يقارب 1500 سطر، وتخزن حالة المستخدمين في الذاكرة فقط. لذلك فإن أكبر فرص التحسين ليست في إضافة ميزات جديدة أولاً، بل في **تثبيت الاعتمادية، حماية المدخلات، والتحكم في التكلفة والموارد**.

## نقاط القوة الحالية

- وجود دعم متعدد المصادر: نص، صور مجمعة، صوت، YouTube، PDF وHTML.
- وجود fallback لعدة مفاتيح Gemini وعدة نماذج.
- وجود فحص أولي للمخرجات قبل تحويل HTML إلى PDF (`reviewGeneratedLesson`).
- استخدام متصفح Puppeteer مشترك مع معالجة لإعادة الاتصال عند إغلاق الهدف.
- وجود Health Check مناسب مبدئياً لـ Railway على `/health`.
- وجود قواعد واضحة نسبياً لعزل مصدر الكويز والتحقق من `sourceEvidence` وعدم تكرار الأسئلة.
- وجود `package-lock.json` وملف `.env.example` و`.gitignore` مناسبين بشكل عام.

## المشاكل المؤكدة حسب الأولوية

### أولوية P0 — أمنية

| المشكلة | الدليل | الأثر | الإجراء المقترح |
|---|---|---|---|
| تشغيل البوت متاح للجميع افتراضياً | `adminControl.js:7-10` يعيد `true` إذا لم يوجد `ALLOWED_USER_IDS` أو `ADMIN_IDS` | أي شخص يستطيع استهلاك Gemini وطلب توليد PDFs وكويزات، ما قد يسبب تكلفة واستنزاف الحصة | اجعل الوضع الافتراضي مغلقاً في الإنتاج: فشل الإقلاع إذا لم توجد قائمة مستخدمين، أو استخدم `REQUIRE_AUTH=true` مع رفض كل غير المصرح لهم |
| قبول HTML يرسله المستخدم ثم تمريره إلى Chromium | `index.js:1266-1291` و`1359-1405` يعالجان HTML مرفوعاً أو مرسلاً مباشرة | احتمال HTML/JavaScript غير موثوق، استنزاف موارد Chromium، وطلبات شبكية داخل الصفحة؛ كما أن `--disable-web-security` يزيد الخطر | إزالة JavaScript قبل العرض، استخدام sanitizer بقائمة سماح، منع `script`, `iframe`, `object`, `embed`, و`url()` الخارجية، وإلغاء `--disable-web-security` إلا لسبب موثق |
| تسريب تفاصيل أخطاء داخل رسائل Telegram | أمثلة في `index.js:798`, `865`, `1071`, `1147`, `1177`, `1321` | قد تظهر للمستخدم تفاصيل داخلية أو معلومات عن الخدمات | أرسل رسالة عامة للمستخدم، وسجّل التفاصيل داخلياً مع `requestId` |
| لا توجد حدود لحجم الملفات أو عدد الصور | معالجات الصور والصوت والوثائق في `index.js:1080+` و`1152+` و`1353+` | استهلاك ذاكرة كبير، OOM، أو إبطاء/إسقاط الخدمة عند رفع ملفات ضخمة | فرض حدود للحجم والعدد والمدة، وفحص `file_size` وMIME الحقيقي قبل تنزيل المحتوى |

### أولوية P1 — اعتمادية وتشغيل

| المشكلة | الدليل | الأثر | الإجراء المقترح |
|---|---|---|---|
| حالة المستخدم محفوظة في `Map` داخل الذاكرة | `index.js:98-119` | فقدان المكتبة والإعدادات بعد restart أو deploy، وعدم مشاركة الحالة بين أكثر من instance | نقل الجلسات والمكتبة إلى Redis أو PostgreSQL؛ استخدم TTL وحداً لحجم HTML والنصوص |
| التزامن غير مضبوط لكل مستخدم | عدة مسارات تستدعي Gemini وPuppeteer مباشرة، مع heartbeat لكل طلب | طلبات متزامنة لنفس المستخدم قد تخلط `lastHtml` أو تثقل Chromium | إضافة queue لكل chat/user مع حد تزامن 1، وحد عام للخدمة، ورفض/تأجيل الطلبات الزائدة |
| `setInterval` للـ heartbeat لا يظهر له تنظيف مضمون في المسار العام | `index.js:961-966`، بينما الإيقاف ليس ظاهراً في المقتطفات المقروءة قبل الخروج من المعالجة | احتمال بقاء مؤقتات وإرسال chat actions بعد انتهاء الطلب أو عند الخطأ | استخدام `try/finally` مع `clearInterval(heartbeatTimer)` دائماً |
| سياسة إعادة المحاولة واسعة ومكلفة | `aiService.js:44-58` و`quizGenerator.js:113-135` يجربان عدة نماذج ومفاتيح؛ كما أن فشل التحقق من جودة الكويز يعيد المحاولة | طلب واحد قد يتحول إلى عدد كبير من استدعاءات Gemini، مع زيادة الزمن والتكلفة | تصنيف الأخطاء: أعد المحاولة فقط مع 429/503/timeout، وحد أقصى للمحاولات، وbackoff مع jitter، وعدم تجربة نموذج غير مدعوم بلا تحقق |
| التطبيق يستمر مع `DUMMY_TOKEN` ثم يفشل تشغيل البوت | `index.js:25-35` و`1490-1500` | Health Check قد يعطي 200 رغم أن البوت غير عامل | في الإنتاج افشل الإقلاع إذا لم يوجد token صحيح؛ اجعل `/health` يعيد حالة غير جاهز عند فشل Telegram، مع `/ready` منفصل |
| استخدام `fetch` لتنزيل ملفات Telegram دون فحص `response.ok` أو timeout | `index.js:1097-1102`, `1156-1161`, `1364-1367` وغيرها | قد تتحول استجابة خطأ إلى Buffer أو تنتظر بلا حد واضح | إضافة `AbortController`, فحص status/content-length، وحد زمني وحجمي |

### أولوية P1 — جودة النتائج

| المشكلة | الدليل | الأثر | الإجراء المقترح |
|---|---|---|---|
| التحقق من HTML سطحي | `reviewGeneratedLesson` في `index.js:918-927` يتحقق من البداية والنهاية وبعض الأنماط فقط | قد يمر HTML غير صالح بصرياً أو PDF فيه صفحات مقصوصة/فارغة | إضافة HTML parser، تحقق من عدد الصفحات، فحص وجود العناوين الأساسية، واختبار render بصري/لقطات أو قياسات DOM قبل الإرسال |
| لا توجد معالجة PDF المصوّر | `sourceExtractor.js:35-49` يرفض PDF بلا نص | ملفات الكتب المصورة الشائعة لن تعمل | إضافة OCR اختياري أو تحويل صفحات PDF إلى صور وإرسالها إلى مسار الصور، مع حد للصفحات |
| استخراج YouTube يعتمد على transcript فقط | `sourceExtractor.js:7-29` | الفيديو الذي لا يملك تفريغاً لن يعمل حتى لو كان الصوت متاحاً | توفير مسار بديل واضح للصوت/التفريغ، وإظهار درجة الثقة ومصدرها بدلاً من نسب دقة ثابتة مثل 8.5/10 |
| ادعاءات الدقة 100% في رسائل المستخدم | موجودة في `README.md` ورسائل `index.js` وprompts | توقعات غير واقعية في محتوى تعليمي مولد | استبدالها برسائل مثل «مراجعة آلية أولية» وطلب مراجعة المعلم للمعادلات والإجابات الحساسة |
| قواعد اكتشاف المادة تعتمد على كلمات مفتاحية فقط | `baccalaureateStandards2027.js` | قد يُصنّف درساً مختلطاً أو قصيراً في المسار الخطأ | إضافة اختيار مادة صريح، وثقة للتصنيف، وسؤال توضيحي عند انخفاض الثقة |

### أولوية P2 — صيانة وجودة كود

- `index.js` يجمع routing، الجلسات، Telegram handlers، تنزيل الملفات، orchestration، health server، وتصدير النتائج. يُفضّل تقسيمه إلى `handlers/`, `services/`, `storage/`, `middleware/`.
- لا توجد اختبارات فعلية؛ الموجود فقط ملف `baccalaureateStandards2027.js` الذي ظهر في بحث أسماء الاختبارات بسبب اسمه، وليس اختباراً.
- لا توجد أوامر `lint`, `test`, أو `format` في `package.json`.
- الاستيراد `Keyboard` و`convertHtmlToPdf` يبدو غير مستخدم في المسارات الحالية؛ يجب تنظيف الاستيرادات أو توثيق سبب الاحتفاظ بها.
- `dotenv.config({ override: true })` قد يطغى على متغيرات البيئة المحقونة في المنصة إذا وُجد ملف `.env` داخل الحاوية؛ الأفضل عدم استخدام `override` في الإنتاج.
- مفتاح API الأساسي `geminiApiKey` يُقرأ مرة واحدة عند بدء التشغيل. إذا تم تدوير متغيرات البيئة أو تغييرها فلن ينعكس ذلك دون restart؛ الأفضل تمرير pool أو قراءته عبر مدير مركزي مع health state.
- أسماء النماذج fallback مثل `gemini-3.5-flash` و`gemini-3.6-flash` ينبغي التحقق من توفرها فعلياً في بيئة الإنتاج بدلاً من افتراضها.

## ملاحظة أمنية مهمة عن Puppeteer

نتيجة `npm audit --omit=dev` الحالية: **4 ثغرات High** مرتبطة بسلسلة `extract-zip` داخل Puppeteer. الإصلاح المقترح آلياً يتضمن ترقية كبرى إلى Puppeteer 25.10.0، لذلك لا أنصح بتشغيل `npm audit fix --force` مباشرة في الإنتاج. الأفضل:

1. ترقية Puppeteer في فرع منفصل.
2. تشغيل اختبارات تحويل PDF على HTML عربي، KaTeX، الصور، والصفحات متعددة A4.
3. مراجعة تغييرات Railway/Nixpacks وحجم Chromium.
4. نشر تدريجي بعد نجاح الاختبارات.

## خطة تنفيذ مقترحة

### المرحلة الأولى — عاجلة، يوم واحد

1. جعل قائمة المستخدمين المسموح لهم إلزامية في الإنتاج.
2. إضافة حدود حجم/عدد الملفات وtimeout لكل تنزيل.
3. تنظيف `heartbeatTimer` باستخدام `finally`.
4. إخفاء تفاصيل الأخطاء عن المستخدم.
5. إزالة JavaScript من HTML المرفوع وتعطيل `--disable-web-security` إن لم يكن ضرورياً.
6. إضافة `npm run check` يشغّل `node --check` لكل ملفات JavaScript و`npm audit --omit=dev` في CI.

### المرحلة الثانية — أسبوع

1. استخراج وحدات Telegram handlers من `index.js`.
2. إضافة Redis/PostgreSQL للجلسات والمكتبة.
3. إضافة queue وحدود تزامن لكل مستخدم.
4. إضافة اختبارات وحدات لـ `extractLessonTitle`, `reviewGeneratedLesson`, `apiKeyManager`, وquiz validators.
5. إضافة مسار OCR للـ PDF المصوّر.
6. إضافة observability: `requestId`, زمن Gemini، زمن Puppeteer، حجم الملفات، وعدد retries.

### المرحلة الثالثة — تحسين المنتج

1. إضافة لوحة إدارة بسيطة للإحصاءات والأخطاء والحصص.
2. تخزين الملفات الناتجة في object storage بدلاً من الاعتماد على الذاكرة فقط.
3. دعم إلغاء الطلب الجاري للمستخدم.
4. إضافة cache للمحتوى المتكرر والكويزات المتطابقة.
5. اعتماد JSON Schema أو structured output للكويز، مع validator صارم قبل الإرسال.
6. إضافة فحص بصري تلقائي للـ PDF لمنع القص والصفحات البيضاء قبل الإرسال.

## التقييم العام

| المحور | التقييم التقريبي | التعليق |
|---|---:|---|
| الفكرة والوظائف | 8/10 | نطاق غني وملائم لمنتج تعليمي فعلي |
| جودة التنظيم | 5/10 | وظائف كثيرة داخل ملف دخول واحد |
| الاعتمادية | 5/10 | توجد محاولات حماية، لكن الحالة والموارد غير معزولة |
| الأمان | 4/10 | السماح الافتراضي وHTML غير الموثوق وغياب حدود الملفات نقاط مهمة |
| قابلية التوسع | 3/10 | الذاكرة المحلية وغياب queue/storage خارجي يحدان التوسع |
| الاختبارات والتسليم | 2/10 | لا توجد suite أو lint أو CI ظاهر |

**الخلاصة:** المشروع قابل للتشغيل ويحتوي على أساس منتج قوي، لكن قبل التوسع في الميزات يجب تنفيذ إصلاحات P0/P1، خصوصاً التحكم في الوصول، تعقيم HTML، حدود الملفات، تخزين الجلسات خارج الذاكرة، وإضافة الاختبارات. هذه التغييرات ستقلل الأعطال والتكلفة وتحمي البوت من إساءة الاستخدام أكثر من إضافة ميزات جديدة حالياً.

## نطاق التحليل

تمت مراجعة المستودع `amaer3011-cmd/nwebot-railway`، وبنيته وملفات JavaScript وإعدادات Railway وREADME ومتغيرات البيئة وسجل Git، مع تشغيل `npm ci --omit=dev` وفحوصات syntax وتشغيل تجريبي بدون token حقيقي. لم يتم تنفيذ طلبات Telegram أو Gemini حقيقية لعدم توفر بيانات اعتماد تشغيلية في بيئة التحليل.

